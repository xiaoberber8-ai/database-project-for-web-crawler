# 爬虫数据管理系统 - 用户操作手册

> **适用对象**：已安装 MySQL 数据库、Python 3.10+ 和 Node.js 18+ 的用户
> **项目文件**：后端 `爬虫数据库系统/`（含 `CRAWLER_NEW.py`）、前端 `crawler-frontend/`（含 Vue 3 工程）

---

## 目录

1. [系统概述](#1-系统概述)
2. [环境准备与依赖安装](#2-环境准备与依赖安装)
3. [MySQL 数据库配置](#3-mysql-数据库配置)
4. [项目配置文件修改](#4-项目配置文件修改)
5. [系统部署与启动](#5-系统部署与启动)
6. [数据爬取操作指南](#6-数据爬取操作指南)
7. [数据管理与检索](#7-数据管理与检索)
8. [功能测试与验证](#8-功能测试与验证)
9. [常见问题排查](#9-常见问题排查)

---

## 1. 系统概述

### 1.1 系统简介

本系统是一个网络爬虫数据管理平台，提供从爬虫策略配置、数据采集到数据检索的全流程管理能力。用户可以通过可视化 Web 界面创建爬虫策略、控制爬虫执行、管理采集数据并进行内容检索。

### 1.2 系统架构

系统采用前后端分离架构，包含 4 个运行组件：

```
浏览器 (localhost:3002)
    │
    ├── /api/* 请求 ──→ Vite 代理 ──→ 后端 API (localhost:8000) ──→ MySQL
    │                                                     ↑
    └── /data-api/* 请求 ──→ Vite 代理 ──→ 数据代理 (localhost:8004) ─┘
                                            (localhost:3306)
```

| 组件 | 端口 | 技术栈 | 作用 |
|------|------|--------|------|
| **前端界面** | 3002 | Vue 3 + Element Plus + Vite | 用户操作入口 |
| **后端 API** | 8000 | FastAPI + SQLAlchemy + BeautifulSoup | 爬虫控制、策略管理、任务调度 |
| **数据代理** | 8004 | FastAPI + PyMySQL | 数据查询、删除、级联操作 |
| **MySQL** | 3306 | MySQL 8.0 | 数据存储 |

### 1.3 核心功能

- **爬虫策略管理**：支持宝宝策略（快速模式）和专业模式（完整配置）
- **爬虫执行控制**：启动、暂停、恢复、停止，实时状态监控
- **数据采集**：BFS 广度优先爬取，支持文本/图片提取、JS 渲染、数据清洗
- **数据管理**：浏览、删除、批量操作、Excel 导出
- **内容检索**：多条件搜索（关键词/发布者/时间/域名），图文展示

---

## 2. 环境准备与依赖安装

### 2.1 前置软件要求

| 软件 | 最低版本 | 推荐版本 | 用途 |
|------|---------|---------|------|
| Python | 3.8 | 3.10+ | 运行后端和数据代理 |
| Node.js | 18.0 | 20 LTS | 运行前端构建工具 |
| MySQL | 8.0 | 8.0.45 | 数据存储 |
| 浏览器 | Chrome 90+ | Chrome 最新版 | 访问 Web 界面 |

### 2.2 Python 依赖安装

打开终端，执行以下命令：

```bash
# 升级 pip
python -m pip install --upgrade pip

# 安装后端依赖
pip install fastapi==0.115.0 uvicorn==0.30.0 sqlalchemy==2.0.0 pymysql==1.1.0 \
            requests==2.32.0 beautifulsoup4==4.12.0 lxml==5.0.0 \
            pydantic==2.7.0 Pillow==10.0.0 cryptography pycryptodome
```

**依赖说明**：

| 库名 | 作用 |
|------|------|
| `fastapi` | Web 框架，提供 RESTful API |
| `uvicorn` | ASGI 服务器 |
| `sqlalchemy` | ORM 框架，操作数据库 |
| `pymysql` | MySQL 驱动 |
| `requests` | HTTP 请求库，抓取网页 |
| `beautifulsoup4` + `lxml` | HTML 解析 |
| `pydantic` | 数据校验 |
| `Pillow` | 图片处理，识别图片尺寸 |
| `cryptography` | MySQL 8.0 认证插件 |
| `pycryptodome` | AES 解密（搜狐等加密图片 URL） |

> **Playwright（可选）**：如需使用 JS 渲染功能，还需安装：
> ```bash
> pip install playwright
> playwright install chromium
> ```

### 2.3 Node.js 依赖安装

```bash
# 进入前端目录
cd crawler-frontend

# 安装前端依赖（首次运行需要，约 1-2 分钟）
npm install
```

如果安装缓慢，可切换国内镜像源：
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

---

## 3. MySQL 数据库配置

### 3.1 启动 MySQL 服务

**Windows**：
```cmd
# 以管理员身份运行
net start MySQL80
```

**Linux/WSL**：
```bash
sudo service mysql start
# 或
sudo systemctl start mysql
```

### 3.2 创建数据库

登录 MySQL 后执行：

```sql
-- 创建数据库（utf8mb4 支持中文和 emoji）
CREATE DATABASE IF NOT EXISTS crawler_db
    DEFAULT CHARACTER SET utf8mb4
    DEFAULT COLLATE utf8mb4_unicode_ci;

-- 验证
SHOW DATABASES LIKE 'crawler_db';
```

### 3.3 数据表自动创建

**无需手动导入 SQL 脚本。** 后端服务首次启动时会自动创建全部 9 张数据表：

```
admin, crawler_strategy, task_record, website, webpage,
content, image, datasource, system_setting
```

自动建表由 `CRAWLER_NEW.py` 中的 `Base.metadata.create_all(engine)` 实现。

### 3.4 配置用户权限（可选）

如需创建专用用户（生产环境推荐）：

```sql
CREATE USER 'crawler_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON crawler_db.* TO 'crawler_user'@'localhost';
FLUSH PRIVILEGES;
```

---

## 4. 项目配置文件修改

### 4.1 后端数据库连接配置

**文件**：`爬虫数据库系统/CRAWLER_NEW.py`

找到数据库连接配置（约第 57 行），修改为你本地的信息：

```python
DATABASE_URL = os.getenv(
    "DATABASE_URL",
    "mysql+pymysql://root:你的密码@127.0.0.1:3306/crawler_db?charset=utf8mb4",
)
```

| 参数 | 说明 | 示例 |
|------|------|------|
| `root` | 数据库用户名 | `root` 或 `crawler_user` |
| `你的密码` | 数据库密码 | 你设置的密码 |
| `127.0.0.1` | 数据库地址 | 本地用 `127.0.0.1` |
| `3306` | 数据库端口 | 默认 3306 |

> **提示**：也可通过环境变量 `DATABASE_URL` 覆盖，无需修改代码。

### 4.2 数据代理配置

**文件**：`crawler-frontend/data_proxy.py`

找到数据库配置（约第 21 行），修改为你本地的信息：

```python
DB_CONFIG = {
    "host": os.environ.get("DB_HOST", "127.0.0.1"),   # 改为本地地址
    "port": int(os.environ.get("DB_PORT", 3306)),
    "user": os.environ.get("DB_USER", "root"),
    "password": os.environ.get("DB_PASSWORD", "你的密码"),
    "database": os.environ.get("DB_NAME", "crawler_db"),
    "charset": "utf8mb4"
}
```

> **提示**：也可通过环境变量 `DB_HOST`、`DB_PASSWORD` 等覆盖。

### 4.3 前端代理配置（一般无需修改）

**文件**：`crawler-frontend/vite.config.js`

```javascript
server: {
    port: 3002,                    // 前端端口
    proxy: {
      '/api': { target: 'http://127.0.0.1:8000', ... },   // 后端 API
      '/data-api': { target: 'http://127.0.0.1:8004', ... }, // 数据代理
      '/images': { target: 'http://127.0.0.1:8000', ... }   // 图片访问
    }
}
```

如果后端服务运行在默认端口（8000/8004），此处无需修改。

---

## 5. 系统部署与启动

### 5.1 启动顺序

系统需要启动 **3 个服务**，建议按以下顺序操作。打开 **3 个终端** 分别运行：

#### 终端 1：启动后端 API 服务（端口 8000）

```bash
cd 爬虫数据库系统
python CRAWLER_NEW.py
```

**验证启动成功**：

终端显示以下日志表示成功：
```
INFO:     Started server process [xxxx]
[startup] 清理了 X 个孤儿任务     # 如有上次未完成的任务
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

#### 终端 2：启动数据代理服务（端口 8004）

```bash
cd crawler-frontend
python data_proxy.py
```

**验证启动成功**：
```
INFO:     Uvicorn running on http://0.0.0.0:8004 (Press CTRL+C to quit)
```

#### 终端 3：启动前端界面（端口 3002）

```bash
cd crawler-frontend
npm run dev
```

**验证启动成功**：
```
  VITE v5.4.x  ready in xxx ms

  ➜  Local:   http://localhost:3002/
  ➜  Network: http://192.168.x.x:3002/
```

### 5.2 访问系统

1. 打开浏览器（推荐 Chrome 或 Edge）
2. 访问地址：**http://localhost:3002**
3. 系统自动跳转到登录页

### 5.3 登录系统

- **用户名**：任意非空字符串（如 `admin`）
- **密码**：任意非空字符串（如 `admin123`）

> 当前为前端简单校验，输入任意非空用户名密码即可登录。登录后跳转到仪表盘。

### 5.4 快速启动清单

将以下命令分别粘贴到 3 个终端中即可完成启动：

```bash
# ========== 终端 1：后端 ==========
cd 爬虫数据库系统 && python CRAWLER_NEW.py

# ========== 终端 2：数据代理 ==========
cd crawler-frontend && python data_proxy.py

# ========== 终端 3：前端 ==========
cd crawler-frontend && npm run dev
```

**三个服务必须同时运行**，缺一不可。

---

## 6. 数据爬取操作指南

### 6.1 创建爬虫策略

1. 登录后，在左侧菜单点击 **"爬虫管理"**
2. 点击右上角 **"新建策略"** 按钮
3. 选择策略类型：

#### 6.1.1 宝宝策略（快速模式）

适合初学者，仅需填写两项：

| 字段 | 说明 | 示例 |
|------|------|------|
| 策略名称 | 策略标识名 | `新浪新闻速采` |
| 目标 URL | 要爬取的网址 | `https://news.sina.com.cn/xxx.shtml` |

其余参数使用默认值（深度 1、不下载图片、标准选择器）。

#### 6.1.2 专业模式（完整配置）

适合高级用户，可精细控制所有参数：

| 字段 | 说明 | 示例 | 默认值 |
|------|------|------|--------|
| 策略名称 | 策略标识名 | `新浪新闻深度爬取` | — |
| 目标 URL | 爬取起始地址 | `https://news.sina.com.cn/xxx.shtml` | — |
| 爬取深度 | BFS 遍历层数 | `1`（只爬当前页）/ `2`（爬+链接页） | 1 |
| 允许域名 | 限制爬取范围 | `news.sina.com.cn` | 空（不限） |
| 标题选择器 | CSS 选择器定位标题 | `h1` | `h1` |
| 正文选择器 | CSS 选择器定位正文 | `.article-content` | `body` |
| 下载图片 | 是否下载页面图片 | 开启 | 否 |
| 图片容器选择器 | 限定图片抓取范围 | `.article-content` | 空 |
| 最小宽/高 | 过滤小图（像素） | `150` | 150 |
| 爬取间隔 | 请求间隔（秒） | `1.0` | 1.0 |
| JS 渲染 | 用 Playwright 渲染 JS 页面 | 关闭 | 否 |
| 去重策略 | skip（跳过）/ overwrite（覆盖） | `overwrite` | overwrite |

4. 填写完成后点击 **"确定"** 保存策略

### 6.2 执行爬取任务

1. 在策略列表中找到目标策略
2. 点击该策略行的 **"启动"** 按钮
3. 系统创建新任务并开始爬取，按钮状态变为"运行中"

> **爬取前检查**：系统会在爬取前自动进行 DNS 解析预检查。如果目标域名无法解析，任务会立即标记为 `failed` 并提示 DNS 错误。

### 6.3 监控任务状态

#### 方式一：爬虫管理页面

策略列表下方显示任务状态卡片：
- **运行状态**：显示爬虫线程是否存活
- **控制按钮**：暂停 / 恢复 / 停止

任务状态说明：

| 状态 | 含义 |
|------|------|
| `running` | 运行中 |
| `paused` | 已暂停 |
| `completed` | 正常完成 |
| `cancelled` | 被用户停止 |
| `failed` | 失败（DNS错误/页面全部失败等） |
| `interrupted` | 服务重启导致中断 |

#### 方式二：仪表盘页面

点击左侧菜单 **"仪表盘"**，可查看：
- 4 个统计卡片：总网页数、总文本数、总图片数、总策略数
- **当前任务指标**（实时刷新，每 3 秒）：
  - 当前任务网页数 / 文本数 / 图片数 / 抓取条目
- 蜘蛛动画：运行中显示搬运数据动画，空闲时显示睡眠动画
- 最近任务列表

### 6.4 控制爬虫执行

| 操作 | 按钮 | 行为 |
|------|------|------|
| 暂停 | **暂停** | 爬虫在当前页面处理完后暂停 |
| 恢复 | **恢复** | 爬虫继续执行 |
| 停止 | **停止** | 爬虫在当前页面处理完后终止，任务标记为 `cancelled` |

### 6.5 管理策略

| 操作 | 说明 |
|------|------|
| **编辑** | 修改已有策略的配置参数 |
| **删除** | 级联删除策略及所有关联数据（任务、网页、内容、图片） |
| **启用/禁用** | 在编辑中修改 Status 字段 |

---

## 7. 数据管理与检索

### 7.1 数据浏览

点击左侧菜单 **"数据管理"**，可切换三个标签页：

#### 网页数据

显示所有爬取的网页记录：
- ID、URL、HTTP 状态码（200 绿色 / 其他红色）、处理状态、任务 ID、爬取时间

#### 文本内容

显示所有采集的文本内容：
- ID、标题、正文预览（前 150 字）、网页 ID
- 操作栏：查看 / 导出 / 删除

#### 图片数据

显示所有采集的图片：
- 缩略图（点击可放大预览）、图片 URL、本地路径、描述、网页 ID
- 操作栏：预览 / 删除

### 7.2 数据删除

| 操作 | 方法 | 说明 |
|------|------|------|
| 单条删除 | 点击操作栏 **"删除"** | 带二次确认弹窗，同步删除数据库记录 |
| 批量删除 | 勾选多条 → 点击 **"批量删除"** | 支持跨页选择，显示选中数量 |

### 7.3 数据导出

文本内容支持导出为 Excel 文件（.xls 格式）：

| 操作 | 方法 | 说明 |
|------|------|------|
| 单条导出 | 点击操作栏 **"导出"** | 导出单条内容 |
| 批量导出 | 勾选多条 → 点击 **"导出选中"** | 导出勾选的多条 |
| 全量导出 | 点击 **"导出全部"** | 导出当前所有内容 |

导出字段：ID、标题、正文内容、网页 ID、爬取时间、策略 ID、发布者、来源 URL。文件可直接用 Excel 打开，中文正常显示。

### 7.4 内容检索

1. 点击左侧菜单 **"内容检索"**
2. 输入搜索条件：

| 条件 | 说明 |
|------|------|
| 关键词 | 模糊匹配标题和正文 |
| 发布者 | 筛选数据来源 |
| 任务 ID | 筛选指定任务采集的内容 |
| 时间范围 | 筛选发布时间 |
| 域名 | 筛选指定网站 |

3. 点击 **"搜索"** 按钮
4. 结果列表显示匹配内容，关键词高亮
5. 点击 **"查看"** 打开详情对话框：
   - 显示完整标题和正文
   - 显示关联图片（通过 webpage_id 关联）
   - 图片支持点击放大预览

---

## 8. 功能测试与验证

### 8.1 后端服务验证

| 验证项 | 操作 | 预期结果 |
|--------|------|---------|
| 后端启动 | `python CRAWLER_NEW.py` | 显示 `Uvicorn running on http://0.0.0.0:8000` |
| 健康检查 | `curl http://127.0.0.1:8000/` | `{"msg":"Crawler DB API is running"}` |
| 策略列表 | `curl http://127.0.0.1:8000/strategies` | 返回 `[]` 或策略列表 |
| 数据表创建 | `mysql -u root -p -e "USE crawler_db; SHOW TABLES;"` | 显示 9 张表 |
| 数据代理启动 | `python data_proxy.py` | 显示 `Uvicorn running on http://0.0.0.0:8004` |
| 数据查询 | `curl http://127.0.0.1:8004/webpages` | 返回 `[]` 或网页列表 |

### 8.2 前端界面验证

| 验证项 | 操作 | 预期结果 |
|--------|------|---------|
| 前端启动 | `npm run dev` | 显示 `Local: http://localhost:3002/` |
| 页面访问 | 浏览器打开 `http://localhost:3002` | 跳转到登录页 |
| 登录 | 输入任意非空用户名密码 | 跳转到仪表盘 |
| 仪表盘 | 查看仪表盘页面 | 显示统计卡片和蜘蛛动画 |
| 爬虫管理 | 点击"爬虫管理" | 显示策略列表 |
| 数据管理 | 点击"数据管理" | 显示三个标签页 |
| 内容检索 | 点击"内容检索" | 显示搜索界面 |

### 8.3 完整爬取流程测试

执行以下步骤，验证系统完整功能：

#### 步骤 1：创建策略

1. 进入"爬虫管理"，点击"新建策略"
2. 选择"宝宝策略"
3. 填入一个可访问的 URL（如 `https://www.baidu.com`）
4. 保存策略

#### 步骤 2：启动爬取

1. 在策略列表点击"启动"按钮
2. 等待任务状态变为 `completed`

#### 步骤 3：验证数据采集

1. 进入"仪表盘"，确认统计数字已更新（网页数 ≥ 1）
2. 进入"数据管理"：
   - **网页数据**标签：确认有新记录，`process_status` 为 `parsed`
   - **文本内容**标签：确认有内容记录，点击"查看"可看全文
   - **图片数据**标签：如果策略开启了图片下载，确认有图片记录

#### 步骤 4：验证数据管理

1. 在"文本内容"标签，勾选多条记录
2. 点击"批量删除"，确认删除成功
3. 点击"导出全部"，确认下载的 .xls 文件可用 Excel 打开
4. 在"图片数据"标签，点击"预览"，确认图片可正常显示

#### 步骤 5：验证内容检索

1. 进入"内容检索"
2. 在关键词输入框输入已采集内容中的关键词
3. 点击"搜索"，确认结果列表正确显示
4. 点击"查看"，确认详情对话框显示标题、正文和关联图片

#### 步骤 6：数据库验证

```sql
USE crawler_db;

-- 查看各表数据量
SELECT '网页' AS 表, COUNT(*) AS 数量 FROM webpage
UNION ALL SELECT '内容', COUNT(*) FROM content
UNION ALL SELECT '图片', COUNT(*) FROM image
UNION ALL SELECT '任务', COUNT(*) FROM task_record;

-- 查看最新任务
SELECT id, strategy_id, Status, item_count, error_message, start_time, end_time
FROM task_record ORDER BY id DESC LIMIT 5;
```

### 8.4 测试注意事项

1. **确保三个服务同时运行**：后端（8000）、数据代理（8004）、前端（3002）
2. **确保 DNS 正常**：如爬取失败提示 DNS 错误，检查 `/etc/resolv.conf` 配置
3. **首次测试建议用简单网站**：如 `https://www.baidu.com`，避免选择器问题干扰
4. **图片下载需要网络**：确保能访问图片所在的服务器
5. **JS 渲染需要 Playwright**：如使用 JS 渲染功能，需提前安装 Playwright 和 Chromium

---

## 9. 常见问题排查

### 9.1 数据库连接失败

**现象**：后端启动报错 `Can't connect to MySQL server`

**排查步骤**：

1. 确认 MySQL 服务已启动：
   ```bash
   # Windows
   net start MySQL80
   # Linux
   sudo service mysql status
   ```

2. 测试数据库连接：
   ```bash
   mysql -u root -p -h 127.0.0.1 -P 3306
   ```

3. 检查配置文件中的地址、端口、密码是否正确（见第 4 节）

4. MySQL 8.0 认证插件问题：
   ```sql
   ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '你的密码';
   FLUSH PRIVILEGES;
   ```

### 9.2 端口冲突

**现象**：启动报错 `Address already in use`

**解决方法**：

```bash
# 查看端口占用
# Linux/Mac
lsof -i :8000
# Windows
netstat -ano | findstr :8000

# 结束占用进程
# Linux/Mac
kill -9 <PID>
# Windows
taskkill /F /PID <PID>
```

或修改端口：
- 后端端口：修改 `CRAWLER_NEW.py` 最后的 `port=8000`
- 数据代理端口：设置环境变量 `DATA_PROXY_PORT=8005`
- 前端端口：修改 `vite.config.js` 中的 `port: 3002`

### 9.3 爬取任务无数据

**现象**：任务状态显示 `completed` 或 `failed`，但抓取条目为 0

**排查步骤**：

1. **查看任务错误信息**：
   - 在"爬虫管理"页面的任务列表查看 `error_message` 字段
   - 或查询数据库：`SELECT id, Status, error_message FROM task_record ORDER BY id DESC LIMIT 5;`

2. **DNS 解析问题**（最常见）：
   - 错误信息含 `Failed to resolve` 或 `Temporary failure in name resolution`
   - 检查 DNS 配置：`cat /etc/resolv.conf`
   - 修复方法（Linux/WSL）：
     ```bash
     echo -e "nameserver 8.8.8.8\nnameserver 114.114.114.114" | sudo tee /etc/resolv.conf
     ```
   - 验证：`python3 -c "import socket; print(socket.gethostbyname('www.baidu.com'))"`

3. **目标网站拒绝访问**：
   - 测试 URL 可达性：`curl -I 目标URL`
   - 如返回 403，尝试在策略配置中修改 User-Agent

4. **CSS 选择器不匹配**：
   - 在浏览器中打开目标 URL，按 F12 检查选择器
   - 确认 `title_selector` 和 `body_selector` 能定位到内容

5. **查看后端日志**：运行 CRAWLER_NEW.py 的终端会输出详细日志

### 9.4 前端页面白屏

**现象**：访问 `http://localhost:3002` 显示空白

**排查步骤**：

1. 按 F12 打开浏览器控制台，查看错误信息
2. 确认后端服务已启动：`curl http://127.0.0.1:8000/`
3. 确认数据代理已启动：`curl http://127.0.0.1:8004/webpages`
4. 清除浏览器缓存：`Ctrl + Shift + Delete`

### 9.5 前端依赖安装失败

**现象**：`npm install` 报错

**解决方法**：

```bash
# 切换国内镜像源
npm config set registry https://registry.npmmirror.com

# 清理缓存重试
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### 9.6 Python 依赖缺失

**现象**：启动报错 `ModuleNotFoundError: No module named 'xxx'`

**解决方法**：

```bash
# 逐一安装缺失模块
pip install 模块名

# 或一次性重装所有依赖
pip install fastapi uvicorn sqlalchemy pymysql requests beautifulsoup4 lxml pydantic Pillow cryptography pycryptodome
```

### 9.7 图片未抓取

**现象**：任务完成但图片数为 0

**排查步骤**：

1. 确认策略中"下载图片"开关已开启
2. 检查"最小宽度/高度"是否设置过大（默认 150px）
3. 部分网站使用图片懒加载加密，系统已内置 `imgsList` 解析逻辑
4. 确认能访问图片所在服务器（部分图片有防盗链）

### 9.8 删除策略报错

**现象**：删除策略时提示 500 错误

**原因**：策略有关联数据，受外键约束

**解决方法**：系统已提供级联删除接口。如仍报错，检查数据代理服务（8004 端口）是否正常运行。

---

## 附录：系统端口速查表

| 服务 | 默认端口 | 启动命令 | 验证命令 |
|------|---------|---------|---------|
| 后端 API | 8000 | `python CRAWLER_NEW.py` | `curl http://127.0.0.1:8000/` |
| 数据代理 | 8004 | `python data_proxy.py` | `curl http://127.0.0.1:8004/webpages` |
| 前端界面 | 3002 | `npm run dev` | 浏览器访问 `http://localhost:3002` |
| MySQL | 3306 | 系统服务 | `mysql -u root -p` |

---

*本手册基于项目当前版本编写。如遇未涵盖的问题，请参考代码文档或查看后端终端日志。*
