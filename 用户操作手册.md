# 爬虫数据管理系统 - 用户操作手册

> **适用对象**：已在 Windows 本地安装 MySQL 数据库，并将以下两个项目文件下载到 VS Code 中的用户
> - 后端项目：`爬虫数据库系统/`（含 `CRAWLER_NEW.py`）
> - 前端项目：`crawler-frontend/`（含 Vue 3 工程）

---

## 目录

1. [系统架构与组件说明](#1-系统架构与组件说明)
2. [环境准备与依赖安装](#2-环境准备与依赖安装)
3. [MySQL 数据库配置](#3-mysql-数据库配置)
4. [项目配置文件修改](#4-项目配置文件修改)
5. [后端服务启动流程](#5-后端服务启动流程)
6. [前端界面运行指南](#6-前端界面运行指南)
7. [爬虫工具使用说明](#7-爬虫工具使用说明)
8. [常见问题排查与解决](#8-常见问题排查与解决)
9. [验证标准](#9-验证标准)

---

## 1. 系统架构与组件说明

本系统采用前后端分离架构，共包含 4 个运行组件：

| 组件 | 端口 | 技术栈 | 作用 |
|------|------|--------|------|
| **前端界面** | 3000 | Vue 3 + Element Plus + Vite | 用户操作入口，提供可视化界面 |
| **后端 API** | 8000 | FastAPI + SQLAlchemy + BeautifulSoup | 爬虫控制、策略管理、任务调度 |
| **数据代理** | 8004 | FastAPI + PyMySQL | 为前端提供数据查询服务（webpage/content/image） |
| **MySQL 数据库** | 3306 | MySQL 8.0 | 存储策略、任务、网页、内容、图片等数据 |

**数据流向**：
```
浏览器 (localhost:3000)
    ↓ /api/* 请求
前端 Vite 代理 ──→ 后端 API (localhost:8000) ──→ MySQL
    ↓ /data-api/* 请求                                  ↑
    └────────────→ 数据代理 (localhost:8004) ──────────┘
```

---

## 2. 环境准备与依赖安装

### 2.1 前置软件要求

| 软件 | 最低版本 | 推荐版本 | 用途 |
|------|---------|---------|------|
| Python | 3.8 | 3.10+ | 运行后端和数据代理 |
| Node.js | 18.0 | 20 LTS | 运行前端构建工具 |
| MySQL | 8.0 | 8.0.45 | 数据存储 |
| VS Code | 最新版 | 最新版 | 代码编辑与终端操作 |

### 2.2 Python 依赖安装

打开 VS Code 终端（`Ctrl + ~`），执行以下命令安装后端所需依赖：

```bash
# 升级 pip
python -m pip install --upgrade pip

# 安装后端依赖
pip install fastapi==0.115.0 uvicorn==0.30.0 sqlalchemy==2.0.0 pymysql==1.1.0 \
            requests==2.32.0 beautifulsoup4==4.12.0 lxml==5.0.0 \
            pydantic==2.7.0 Pillow==10.0.0 cryptography
```

**依赖说明**：

| 库名 | 作用 |
|------|------|
| `fastapi` | Web 框架，提供 RESTful API |
| `uvicorn` | ASGI 服务器，运行 FastAPI 应用 |
| `sqlalchemy` | ORM 框架，操作数据库 |
| `pymysql` | MySQL 驱动 |
| `requests` | HTTP 请求库，抓取网页 |
| `beautifulsoup4` | HTML 解析库 |
| `lxml` | 高性能 XML/HTML 解析器（BeautifulSoup 后端） |
| `pydantic` | 数据校验（FastAPI 依赖） |
| `Pillow` | 图片处理，识别图片尺寸 |
| `cryptography` | SQLAlchemy 连接 MySQL 8.0 的认证插件 |

### 2.3 Node.js 依赖安装

```bash
# 进入前端目录
cd crawler-frontend

# 安装前端依赖（首次运行需要，约 1-2 分钟）
npm install
```

**主要前端依赖**（已在 `package.json` 中声明）：
- `vue@^3.4.0` - 前端框架
- `vue-router@^4.3.0` - 路由管理
- `axios@^1.7.0` - HTTP 请求库
- `element-plus@^2.7.0` - UI 组件库
- `vite@^5.4.0` - 构建工具

---

## 3. MySQL 数据库配置

### 3.1 启动 MySQL 服务

在 Windows 服务中确认 MySQL 服务已启动：
- 按 `Win + R`，输入 `services.msc`
- 找到 `MySQL80` 服务，确认状态为"正在运行"

或在命令行启动（以管理员身份）：
```cmd
net start MySQL80
```

### 3.2 登录 MySQL

```bash
mysql -u root -p
```
输入你设置的 root 密码后进入 MySQL 命令行。

### 3.3 创建数据库

```sql
-- 创建数据库（使用 utf8mb4 字符集以支持中文和 emoji）
CREATE DATABASE IF NOT EXISTS crawler_db
    DEFAULT CHARACTER SET utf8mb4
    DEFAULT COLLATE utf8mb4_unicode_ci;

-- 验证创建成功
SHOW DATABASES LIKE 'crawler_db';
```

### 3.4 配置用户权限

**方式一：直接使用 root 用户（开发环境推荐）**

如果你使用 root 用户连接，需确保 root 允许从本机连接。MySQL 8.0 默认 root 用户只能从 localhost 连接，这正好符合我们的需求，无需额外配置。

**方式二：创建专用用户（生产环境推荐）**

```sql
-- 创建专用用户
CREATE USER 'crawler_user'@'localhost' IDENTIFIED BY 'your_password_here';

-- 授予 crawler_db 数据库的全部权限
GRANT ALL PRIVILEGES ON crawler_db.* TO 'crawler_user'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;
```

### 3.5 数据表自动创建

**重要**：无需手动导入 SQL 脚本。后端服务首次启动时会自动创建所有数据表。

后端代码 [CRAWLER_NEW.py#L185](爬虫数据库系统/CRAWLER_NEW.py#L185) 中包含：
```python
Base.metadata.create_all(engine)
```

这会自动创建以下 8 张表：
- `admin` - 管理员账号
- `crawler_strategy` - 爬虫策略
- `task_record` - 任务记录
- `website` - 网站信息
- `webpage` - 网页记录
- `content` - 文本内容
- `image` - 图片记录
- `datasource` - 数据来源
- `system_setting` - 系统设置

### 3.6 验证数据库配置

```sql
USE crawler_db;
SHOW TABLES;
-- 此时表尚未创建，启动后端后会自动生成
```

---

## 4. 项目配置文件修改

### 4.1 后端数据库连接配置

**文件位置**：`爬虫数据库系统/CRAWLER_NEW.py` 第 46-49 行

打开文件，找到以下配置段：

```python
DATABASE_URL = os.getenv(
    "DATABASE_URL",
    "mysql+pymysql://root:zhouwanyi1222@172.31.224.1:3306/crawler_db?charset=utf8mb4",
)
```

**修改为你本地的数据库连接信息**：

| 参数 | 说明 | 示例值 |
|------|------|--------|
| `root` | 数据库用户名 | `root` 或 `crawler_user` |
| `zhouwanyi1222` | 数据库密码 | 你设置的密码 |
| `172.31.224.1` | 数据库主机地址 | `127.0.0.1`（本地）或 `localhost` |
| `3306` | 数据库端口 | `3306`（默认） |
| `crawler_db` | 数据库名 | `crawler_db` |

**Windows 本地用户修改示例**：
```python
DATABASE_URL = os.getenv(
    "DATABASE_URL",
    "mysql+pymysql://root:你的密码@127.0.0.1:3306/crawler_db?charset=utf8mb4",
)
```

> **提示**：也可以不修改代码，通过环境变量覆盖。在启动命令前加：
> ```bash
> # Windows CMD
> set DATABASE_URL=mysql+pymysql://root:你的密码@127.0.0.1:3306/crawler_db?charset=utf8mb4
> # Windows PowerShell
> $env:DATABASE_URL="mysql+pymysql://root:你的密码@127.0.0.1:3306/crawler_db?charset=utf8mb4"
> ```

### 4.2 数据代理服务配置

**文件位置**：`crawler-frontend/data_proxy.py` 第 11-17 行

```python
DB_CONFIG = {
    "host": os.environ.get("DB_HOST", "172.31.224.1"),
    "port": int(os.environ.get("DB_PORT", 3306)),
    "user": os.environ.get("DB_USER", "root"),
    "password": os.environ.get("DB_PASSWORD", "zhouwanyi1222"),
    "database": os.environ.get("DB_NAME", "crawler_db"),
    "charset": "utf8mb4"
}
```

**修改为你本地的数据库信息**：

```python
DB_CONFIG = {
    "host": os.environ.get("DB_HOST", "127.0.0.1"),  # 改为本地地址
    "port": int(os.environ.get("DB_PORT", 3306)),
    "user": os.environ.get("DB_USER", "root"),
    "password": os.environ.get("DB_PASSWORD", "你的密码"),
    "database": os.environ.get("DB_NAME", "crawler_db"),
    "charset": "utf8mb4"
}
```

### 4.3 前端代理配置（一般无需修改）

**文件位置**：`crawler-frontend/vite.config.js`

```javascript
server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://127.0.0.1:8000',  // 后端 API 地址
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      },
      '/data-api': {
        target: 'http://127.0.0.1:8004',  // 数据代理地址
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/data-api/, '')
      }
    }
  }
```

此配置将前端的 `/api/*` 请求转发到后端 8000 端口，`/data-api/*` 请求转发到数据代理 8004 端口。**如果后端和数据代理运行在本机默认端口，无需修改**。

---

## 5. 后端服务启动流程

### 5.1 启动后端 API 服务（端口 8000）

**第一步**：在 VS Code 中打开终端（`Ctrl + ~`）

**第二步**：切换到后端目录
```bash
cd 爬虫数据库系统
```

**第三步**：启动后端服务
```bash
python CRAWLER_NEW.py
```

**第四步**：验证启动成功

终端应显示以下日志：
```
INFO:     Started server process [xxxx]
INFO:     Waiting for application startup.
[startup] 清理了 X 个孤儿任务    # 如果有上次未完成的任务会显示
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

看到 `Uvicorn running on http://0.0.0.0:8000` 即表示后端启动成功。

### 5.2 启动数据代理服务（端口 8004）

**新开一个终端**（点击 VS Code 终端的 `+` 号），执行：

```bash
cd crawler-frontend
python data_proxy.py
```

验证启动成功：
```
INFO:     Started server process [xxxx]
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8004 (Press CTRL+C to quit)
```

### 5.3 验证后端服务可用性

**新开终端**，执行以下命令测试：

```bash
# 测试后端 API 根路径
curl http://127.0.0.1:8000/
# 预期返回: {"msg":"Crawler DB API is running"}

# 测试策略列表接口
curl http://127.0.0.1:8000/strategies
# 预期返回: [] （空数组，表示还没有策略）

# 测试数据代理
curl http://127.0.0.1:8004/webpages
# 预期返回: [] （空数组）
```

---

## 6. 前端界面运行指南

### 6.1 启动前端开发服务器（端口 3000）

**新开第三个终端**，执行：

```bash
cd crawler-frontend
npm run dev
```

> **注意**：首次运行前必须先执行 `npm install` 安装依赖（见 2.3 节）

### 6.2 验证前端启动

终端应显示：
```
  VITE v5.4.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: http://192.168.x.x:3000/
```

### 6.3 访问 Web 界面

1. 打开浏览器（推荐 Chrome 或 Edge）
2. 访问地址：**http://localhost:3000**
3. 系统会自动跳转到登录页：`http://localhost:3000/login`

### 6.4 登录系统

登录页说明：
- **用户名**：任意非空字符串（如 `admin`）
- **密码**：任意非空字符串（如 `admin123`）

> **说明**：当前登录为前端简单校验，输入任意非空用户名和密码即可登录。登录成功后会被重定向到仪表盘页面。

---

## 7. 爬虫工具使用说明

### 7.1 创建爬虫策略

1. 登录后，在左侧菜单点击 **"爬虫管理"**
2. 点击右上角 **"新建策略"** 按钮
3. 填写策略信息：

| 字段 | 说明 | 示例 |
|------|------|------|
| 策略名称 | 策略的标识名 | `搜狐新闻爬取` |
| 目标 URL | 要爬取的起始网址 | `https://www.sohu.com/a/xxxxx` |
| 爬取深度 | 1=只爬当前页，2=爬当前页+链接页 | `1` |
| 标题选择器 | CSS 选择器，定位标题 | `h1.article-title` |
| 正文选择器 | CSS 选择器，定位正文 | `.article-content` |
| 下载图片 | 是否下载页面图片 | 开启 |
| 图片容器选择器 | 限定图片抓取范围 | `.article-content` |
| 最小宽度(px) | 过滤小图 | `150` |
| 最小高度(px) | 过滤小图 | `150` |
| 爬取间隔(秒) | 请求间隔，避免被封 | `1.0` |

4. 点击 **"确定"** 保存策略

### 7.2 执行爬取任务

1. 在策略列表中找到目标策略
2. 点击该策略行的 **"启动"** 按钮
3. 系统会创建一个新任务并开始爬取
4. 按钮状态会变为"运行中"

### 7.3 监控任务状态

**方式一：爬虫管理页面**

策略列表下方会显示任务状态：
- `pending` - 等待中
- `running` - 运行中
- `completed` - 已完成
- `interrupted` - 被中断（服务重启等）
- `failed` - 失败

**方式二：仪表盘页面**

点击左侧菜单 **"仪表盘"**，可查看：
- 总网页数、总文本数、总图片数
- **当前任务指标**（实时更新，每 5 秒刷新）：
  - 当前任务网页数
  - 当前任务文本数
  - 当前任务图片数
  - 当前任务抓取条目
- 最近任务列表

### 7.4 查看爬取结果

**数据管理页面**：

1. 点击左侧菜单 **"数据管理"**
2. 可切换标签页查看：
   - **网页列表**：显示所有爬取的网页（URL、抓取时间、状态）
   - **内容列表**：显示所有文本内容（标题、正文摘要）
   - **图片列表**：显示所有图片（URL、描述）

### 7.5 内容检索

1. 点击左侧菜单 **"内容检索"**
2. 在搜索框输入关键词
3. 可选过滤条件：
   - 按发布时间范围
   - 按数据来源
4. 点击 **"搜索"** 按钮
5. 结果列表显示匹配的内容标题和摘要
6. 点击 **"查看"** 按钮，弹出详情对话框：
   - 显示完整标题和正文
   - 显示关联的图片（通过 webpage_id 关联）
   - 图片支持点击放大预览

### 7.6 管理策略

- **编辑策略**：点击策略行的"编辑"按钮，修改后保存
- **删除策略**：点击策略行的"删除"按钮（会级联删除关联的任务、网页、内容、图片）
- **停止任务**：点击运行中策略的"停止"按钮

---

## 8. 常见问题排查与解决

### 8.1 数据库连接失败

**现象**：后端启动报错 `Can't connect to MySQL server`

**排查步骤**：

1. **确认 MySQL 服务运行中**：
   ```cmd
   net start MySQL80
   ```

2. **测试数据库连接**：
   ```bash
   mysql -u root -p -h 127.0.0.1 -P 3306
   ```
   如果连不上，检查 MySQL 服务状态和端口。

3. **检查配置文件**：
   - 确认 `CRAWLER_NEW.py` 第 48 行的 `DATABASE_URL` 配置正确
   - 确认 `data_proxy.py` 第 11-17 行的 `DB_CONFIG` 配置正确
   - 密码是否正确，地址是否为 `127.0.0.1`（本地）

4. **MySQL 8.0 认证插件问题**：
   ```sql
   -- 登录 MySQL 后执行
   ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '你的密码';
   FLUSH PRIVILEGES;
   ```

### 8.2 端口冲突

**现象**：启动时报错 `Address already in use` 或 `[Errno 98]`

**排查步骤**：

1. **查看端口占用**：
   ```bash
   # Windows CMD
   netstat -ano | findstr :8000
   netstat -ano | findstr :8004
   netstat -ano | findstr :3000
   ```

2. **结束占用进程**：
   ```cmd
   :: 找到 PID 后强制结束
   taskkill /F /PID <PID号>
   ```

3. **或修改端口**：
   - 后端端口：修改 `CRAWLER_NEW.py` 最后的 `port=8000`
   - 数据代理端口：设置环境变量 `DATA_PROXY_PORT=8005`
   - 前端端口：修改 `vite.config.js` 中的 `port: 3000`

### 8.3 前端依赖安装失败

**现象**：`npm install` 报错

**解决方法**：

1. **切换 npm 镜像源**：
   ```bash
   npm config set registry https://registry.npmmirror.com
   npm install
   ```

2. **清理缓存重试**：
   ```bash
   npm cache clean --force
   rm -rf node_modules package-lock.json
   npm install
   ```

3. **使用 pnpm 或 yarn**（如果已安装）：
   ```bash
   pnpm install
   ```

### 8.4 Python 依赖缺失

**现象**：启动报错 `ModuleNotFoundError: No module named 'xxx'`

**解决方法**：

```bash
# 查看缺失的模块名，逐一安装
pip install 模块名

# 或一次性重装所有依赖
pip install fastapi uvicorn sqlalchemy pymysql requests beautifulsoup4 lxml pydantic Pillow cryptography
```

### 8.5 爬取任务无数据

**现象**：任务状态显示 `completed`，但网页/内容/图片数为 0

**排查步骤**：

1. **检查目标 URL 是否可访问**：
   ```bash
   curl -I 目标URL
   ```
   如果返回 403/404，说明网站拒绝访问或 URL 错误。

2. **检查 CSS 选择器是否正确**：
   - 在浏览器中打开目标 URL
   - 按 F12 打开开发者工具
   - 用 Elements 面板验证选择器是否能定位到内容

3. **查看后端日志**：
   终端会输出爬取过程的详细日志，查看是否有 `Failed to fetch` 或 `Selector not found` 等警告。

4. **图片未抓取**：
   - 部分网站（如搜狐）使用图片懒加载加密，真实图片 URL 藏在 JavaScript 变量中
   - 后端已内置 `imgsList` 解析逻辑，支持此类站点
   - 检查"最小宽度/高度"设置是否过大，导致图片被过滤

### 8.6 前端页面白屏

**现象**：访问 `http://localhost:3000` 显示空白

**排查步骤**：

1. **按 F12 打开浏览器控制台**，查看错误信息
2. **确认后端服务已启动**：
   ```bash
   curl http://127.0.0.1:8000/
   ```
3. **确认数据代理已启动**：
   ```bash
   curl http://127.0.0.1:8004/webpages
   ```
4. **清除浏览器缓存**：`Ctrl + Shift + Delete`

### 8.7 删除策略报错 500

**现象**：删除策略时提示 `Request failed with status code 500`

**原因**：策略有关联的任务记录，受外键约束

**解决方法**：系统已提供级联删除接口，前端删除按钮会自动调用。如果仍报错，检查数据代理服务（8004 端口）是否正常运行。

---

## 9. 验证标准

### 9.1 后端服务验证

| 验证项 | 操作方法 | 预期结果 |
|--------|---------|----------|
| 服务启动 | 执行 `python CRAWLER_NEW.py` | 终端显示 `Uvicorn running on http://0.0.0.0:8000` |
| 根路径 | `curl http://127.0.0.1:8000/` | 返回 `{"msg":"Crawler DB API is running"}` |
| 策略接口 | `curl http://127.0.0.1:8000/strategies` | 返回 `[]` 或策略列表 JSON |
| 数据表创建 | `mysql -u root -p -e "USE crawler_db; SHOW TABLES;"` | 显示 9 张表（admin, crawler_strategy, task_record, website, webpage, content, image, datasource, system_setting） |

### 9.2 数据代理验证

| 验证项 | 操作方法 | 预期结果 |
|--------|---------|----------|
| 服务启动 | 执行 `python data_proxy.py` | 终端显示 `Uvicorn running on http://0.0.0.0:8004` |
| 网页查询 | `curl http://127.0.0.1:8004/webpages` | 返回 `[]` 或网页列表 JSON |
| 内容查询 | `curl http://127.0.0.1:8004/contents` | 返回 `[]` 或内容列表 JSON |
| 图片查询 | `curl http://127.0.0.1:8004/images` | 返回 `[]` 或图片列表 JSON |

### 9.3 前端界面验证

| 验证项 | 操作方法 | 预期结果 |
|--------|---------|----------|
| 服务启动 | 执行 `npm run dev` | 终端显示 `Local: http://localhost:3000/` |
| 页面访问 | 浏览器打开 `http://localhost:3000` | 自动跳转到登录页 |
| 登录功能 | 输入任意非空用户名密码 | 登录成功，跳转到仪表盘 |
| 仪表盘 | 查看仪表盘页面 | 显示统计卡片（数值可能为 0） |
| 爬虫管理 | 点击"爬虫管理"菜单 | 显示策略列表（可能为空） |

### 9.4 完整功能验证（首次爬取任务）

执行以下完整流程，验证系统可用性：

1. **创建策略**：
   - 进入"爬虫管理"，点击"新建策略"
   - 填入一个简单的可访问 URL（如 `https://example.com`）
   - 爬取深度设为 1，开启下载图片
   - 保存策略

2. **启动任务**：
   - 点击策略的"启动"按钮
   - 等待任务状态变为 `completed`

3. **验证数据**：
   - 进入"仪表盘"，确认统计数字已更新
   - 进入"数据管理"，确认有网页、内容、图片记录
   - 进入"内容检索"，搜索关键词，点击"查看"确认图文展示正常

4. **数据库验证**：
   ```sql
   USE crawler_db;
   SELECT COUNT(*) FROM webpage;    -- 网页数
   SELECT COUNT(*) FROM content;    -- 内容数
   SELECT COUNT(*) FROM image;      -- 图片数
   SELECT * FROM task_record ORDER BY id DESC LIMIT 1;  -- 最新任务
   ```

---

## 附录：快速启动清单

按照以下顺序执行，即可完成系统启动：

```bash
# ========== 终端 1：启动后端 ==========
cd 爬虫数据库系统
python CRAWLER_NEW.py

# ========== 终端 2：启动数据代理 ==========
cd crawler-frontend
python data_proxy.py

# ========== 终端 3：启动前端 ==========
cd crawler-frontend
npm run dev

# ========== 浏览器 ==========
# 访问 http://localhost:3000
# 输入任意用户名密码登录
# 开始使用系统
```

**三个服务必须同时运行**，缺一不可：
- 后端 API（8000）：爬虫控制与策略管理
- 数据代理（8004）：前端数据查询
- 前端界面（3000）：用户操作入口

---

*本手册基于项目当前版本编写，如遇问题请参考第 8 节常见问题排查。*
