# 爬虫数据库管理系统 - 代码文档

> 本文档详细描述系统的模块结构、接口规范与使用方法。

---

## 一、项目文件结构

```
database-project-for-web-crawler/
├── 爬虫数据库系统/                      # 后端项目
│   ├── CRAWLER_NEW.py                  # 主程序：FastAPI + SQLAlchemy + 爬虫引擎
│   ├── render_service.py               # Playwright 渲染服务
│   ├── images/                         # 爬虫下载的图片存储目录
│   └── .chromium_libs/                 # Chromium 依赖库
│
├── crawler-frontend/                   # 前端项目
│   ├── data_proxy.py                   # 数据查询代理服务
│   ├── vite.config.js                  # Vite 构建配置
│   ├── package.json                    # 前端依赖声明
│   ├── index.html                      # HTML 入口
│   └── src/
│       ├── main.js                     # 应用入口
│       ├── App.vue                     # 根组件
│       ├── api/
│       │   └── index.js                # API 接口封装
│       ├── router/
│       │   └── index.js                # 路由配置
│       ├── components/
│       │   └── SpiderAnimation.vue     # 蜘蛛动画组件
│       └── views/
│           ├── Login.vue               # 登录页
│           ├── Layout.vue              # 主布局
│           ├── Dashboard.vue           # 仪表盘
│           ├── Crawler.vue             # 爬虫管理
│           ├── Data.vue                # 数据管理
│           └── Search.vue              # 内容检索
│
├── README.md                           # 项目简介
└── 用户操作手册.md                       # 用户操作手册
```

---

## 二、模块描述

### 2.1 后端模块

#### 2.1.1 CRAWLER_NEW.py — 主程序模块

**文件路径**：`爬虫数据库系统/CRAWLER_NEW.py`

**职责**：系统的核心后端模块，集成 FastAPI Web 服务、SQLAlchemy ORM 数据模型、爬虫执行引擎于一体。

**模块组成**：

| 子模块 | 行号范围 | 职责 |
|--------|---------|------|
| 导入与配置 | 1-70 | 依赖导入、数据库连接配置、日志配置 |
| 数据模型层 | 74-195 | 9 个 SQLAlchemy ORM 模型定义 |
| Pydantic 校验层 | 203-320 | 8 个请求/响应数据模型 |
| 工具函数 | 325-386 | URL 处理、MD5 哈希、密码验证等 |
| 策略服务 | 388-455 | `StrategyService` 类，策略 CRUD |
| 爬虫规则 | 457-495 | `CrawlRule` 数据类 |
| 爬虫执行器 | 497-1610 | `CrawlerExecutor` 类，爬虫核心引擎 |
| API 路由 | 1615-1787 | FastAPI 路由定义 |

**关键类说明**：

**`CrawlerExecutor`** — 爬虫执行引擎

```python
class CrawlerExecutor:
    def __init__(self):
        self.thread = None              # 爬虫线程
        self.stop_event = threading.Event()  # 停止信号
        self.pause_event = threading.Event() # 暂停信号
        self.session = requests.Session()    # HTTP 会话
        self.current_strategy_id = None      # 当前策略 ID
```

线程安全设计：爬虫在独立线程中运行，通过 `Event` 信号与主线程通信，支持暂停/恢复/停止控制。

**`StrategyService`** — 策略管理服务

提供 5 个静态方法：`create_strategy`、`list_strategies`、`get_strategy`、`update_strategy`、`delete_strategy`，封装策略的完整 CRUD 操作。

#### 2.1.2 render_service.py — 渲染服务模块

**文件路径**：`爬虫数据库系统/render_service.py`

**职责**：基于 Playwright 的网页实时渲染服务，用于处理 JavaScript 动态渲染的页面（如知乎、单页应用）。

**关键类**：

| 类/函数 | 职责 |
|--------|------|
| `LoadStatus` | 渲染状态枚举（SUCCESS/TIMEOUT/ERROR） |
| `RenderResult` | 渲染结果数据类（html/status_code/screenshot） |
| `RenderConfig` | 渲染配置（超时/视口大小/等待选择器） |
| `RenderService` | 渲染服务主类，管理 Playwright 浏览器实例 |
| `get_render_service()` | 获取全局单例 |
| `render_url()` | 便捷函数，一次性渲染 URL |

**渲染流程**：

```
render_page(url)
    ↓
启动 Chromium → 创建页面 → 设置视口 → 导航到 URL
    ↓
等待网络空闲 / 超时 → 获取页面 HTML → 关闭页面
    ↓
返回 RenderResult(html, status_code)
```

**降级策略**：当 Playwright 不可用时，自动降级到 `requests` + JS 变量提取模式。

#### 2.1.3 data_proxy.py — 数据查询代理模块

**文件路径**：`crawler-frontend/data_proxy.py`

**职责**：独立的数据查询代理服务，使用 PyMySQL 直接操作数据库，为前端提供数据浏览、删除和导出接口。

**设计说明**：此服务独立于主后端运行，原因有二：
1. 分离查询负载与爬虫负载，避免数据查询影响爬虫性能
2. 使用 PyMySQL 直接 SQL 查询，绕过 SQLAlchemy ORM，对复杂关联查询更灵活

**查询特性**：`/contents` 接口通过多表 JOIN 一次性获取内容及其关联的爬取时间、任务 ID、策略 ID、发布者信息：

```sql
SELECT c.*, w.fetch_time AS crawl_time, w.task_id,
       t.strategy_id, ds.publisher_name AS Publisher, ds.origin_url
FROM content c
LEFT JOIN webpage w ON c.webpage_id = w.id
LEFT JOIN task_record t ON w.task_id = t.id
LEFT JOIN datasource ds ON c.datasource_id = ds.id
```

### 2.2 前端模块

#### 2.2.1 api/index.js — API 封装模块

**职责**：封装所有 HTTP 请求，提供统一的接口函数。

**两个 axios 实例**：

| 实例名 | baseURL | 代理目标 | 拦截器 |
|--------|---------|---------|--------|
| `api` | `/api` | `localhost:8000` | 成功返回 data，失败弹 ElMessage |
| `dataApi` | `/data-api` | `localhost:8004` | 同上 |

**导出函数清单**：

| 函数名 | HTTP | 路径 | 用途 |
|--------|------|------|------|
| `getStrategies()` | GET | `/strategies` | 获取策略列表 |
| `getStrategy(id)` | GET | `/strategies/{id}` | 获取策略详情 |
| `createStrategy(data)` | POST | `/strategies` | 创建策略 |
| `updateStrategy(id, data)` | PUT | `/strategies/{id}` | 更新策略 |
| `deleteStrategy(id)` | DELETE | `/strategies/{id}` | 删除策略 |
| `startCrawl(strategyId)` | POST | `/crawl/start` | 启动爬虫 |
| `pauseCrawl()` | POST | `/crawl/pause` | 暂停爬虫 |
| `resumeCrawl()` | POST | `/crawl/resume` | 恢复爬虫 |
| `stopCrawl()` | POST | `/crawl/stop` | 停止爬虫 |
| `getCrawlStatus()` | GET | `/crawl/status` | 获取爬虫状态 |
| `getTasks(strategyId)` | GET | `/tasks` | 获取任务列表 |
| `getTask(id)` | GET | `/tasks/{id}` | 获取任务详情 |
| `getWebpages()` | GET | `/webpages` | 获取网页列表 |
| `getContents()` | GET | `/contents` | 获取内容列表 |
| `getImages()` | GET | `/images` | 获取图片列表 |
| `getWebsites()` | GET | `/websites` | 获取网站列表 |
| `deleteContent(id)` | DELETE | `/contents/{id}` | 删除单条内容 |
| `batchDeleteContents(ids)` | POST | `/contents/batch-delete` | 批量删除内容 |
| `deleteImage(id)` | DELETE | `/images/{id}` | 删除单条图片 |
| `batchDeleteImages(ids)` | POST | `/images/batch-delete` | 批量删除图片 |

#### 2.2.2 router/index.js — 路由模块

**职责**：定义前端路由表，实现页面导航和登录守卫。

**路由表**：

| 路径 | 组件 | 需要登录 | 说明 |
|------|------|---------|------|
| `/login` | Login.vue | 否 | 登录页 |
| `/dashboard` | Dashboard.vue | 是 | 仪表盘（默认首页） |
| `/crawler` | Crawler.vue | 是 | 爬虫管理 |
| `/data` | Data.vue | 是 | 数据管理 |
| `/search` | Search.vue | 是 | 内容检索 |

所有组件均使用懒加载（`() => import(...)`），按需加载减少首屏时间。

#### 2.2.3 视图模块

**Login.vue — 登录模块**

- 表单校验：用户名和密码非空
- 登录成功后设置 `localStorage.isLoggedIn = 'true'`
- 重定向到仪表盘

**Layout.vue — 布局模块**

- Element Plus `el-container` 三段式布局：侧边栏 + 顶栏 + 主内容区
- 侧边栏：4 个菜单项（仪表盘/爬虫管理/数据管理/内容检索），支持折叠
- 顶栏：折叠按钮 + 系统标题 + 用户下拉菜单（退出登录）
- 通过 `<router-view>` 渲染子路由

**Dashboard.vue — 仪表盘模块**

- 4 个统计卡片：总网页数、总文本数、总图片数、总策略数
- 当前任务指标区域：实时显示运行中任务的网页/文本/图片/条目数（每 3 秒刷新）
- 蜘蛛动画组件：运行中显示搬运数据动画，空闲时显示睡眠动画
- 最近任务列表：显示最近 5 条任务记录

**Crawler.vue — 爬虫管理模块**

- 爬虫状态卡片：显示运行状态、当前策略、控制按钮（启动/暂停/恢复/停止）
- 策略列表表格：显示所有策略，支持编辑/删除/启动操作
- 任务列表表格：显示任务记录，支持按策略筛选
- 策略创建对话框：两种模式（宝宝策略/专业模式）
  - 宝宝策略：仅需名称和 URL
  - 专业模式：完整配置（深度/选择器/图片规则/请求参数/JS渲染）

**Data.vue — 数据管理模块**

- 三个标签页：网页数据 / 文本内容 / 图片数据
- 网页数据：显示 URL、HTTP 状态、处理状态、爬取时间
- 文本内容：支持查看详情、单条删除、批量删除、单条导出、批量导出、全量导出（Excel）
- 图片数据：缩略图预览、点击放大、单条删除、批量删除
- 分页：每页 15 条

**Search.vue — 内容检索模块**

- 搜索条件：关键词、发布者、任务 ID、时间范围、域名
- 结果列表：标题 + 正文摘要（关键词高亮）
- 详情对话框：完整标题 + 正文 + 关联图片（支持点击放大）

#### 2.2.4 components/SpiderAnimation.vue — 动画组件

**职责**：纯 CSS 实现的蜘蛛搬运数据动画，增强仪表盘的视觉效果。

**动画状态**：

| 状态 | 触发条件 | 动画效果 |
|------|---------|---------|
| 运行中 | 爬虫线程存活 | 蜘蛛从左到右行走，背上搬运数据方块，8 条腿交替摆动 |
| 睡眠 | 爬虫线程不存活 | 蜘蛛静止呼吸，头顶显示 `zzz` 浮动，状态点变灰 |

**实现技术**：纯 CSS keyframes 动画，无 JavaScript 逻辑，通过 props 控制状态切换。

---

## 三、接口说明

### 3.1 后端 API 接口（端口 8000）

#### 3.1.1 健康检查

```
GET /
```

**响应**：
```json
{"msg": "Crawler DB API is running"}
```

#### 3.1.2 策略管理接口

##### 创建策略

```
POST /strategies
Content-Type: application/json
```

**请求体**：
```json
{
  "name": "新浪新闻爬取",
  "target_url": "https://news.sina.com.cn/zx/2026-06-30/doc-xxx.shtml",
  "rules": {
    "strategy_type": "professional",
    "depth": 1,
    "allowed_domains": [],
    "start_urls": [],
    "text_rules": {
      "title_selector": "h1",
      "body_selector": "body",
      "company_selector": null,
      "contact_selector": null,
      "source_selector": null
    },
    "image_rules": {
      "image_selector": "img",
      "download_images": true,
      "image_dir": "./images",
      "image_container_selector": null,
      "min_width": 150,
      "min_height": 150,
      "min_area": 30000,
      "max_ratio": 5.0,
      "exclude_keywords": ["ad", "banner", "logo", "icon"],
      "only_article_images": true
    },
    "headers": {"User-Agent": "Mozilla/5.0"},
    "timeout": 30,
    "rate_limit": 1.0,
    "duplicate_action": "overwrite",
    "use_js_render": false
  },
  "Status": "enabled",
  "Frequency": "manual",
  "creator_id": null
}
```

**响应**（200）：
```json
{
  "id": 1,
  "name": "新浪新闻爬取",
  "target_url": "https://news.sina.com.cn/zx/...",
  "rules": { ... },
  "Status": "enabled",
  "Frequency": "manual",
  "creator_id": null,
  "created_at": "2026-06-30T19:47:56",
  "updated_at": "2026-06-30T19:47:56"
}
```

##### 获取策略列表

```
GET /strategies
```

**响应**：`StrategyOut[]` — 策略数组，按 ID 降序排列。

##### 获取策略详情

```
GET /strategies/{strategy_id}
```

**路径参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| `strategy_id` | int | 策略 ID |

**响应**（200）：`StrategyOut`  
**错误**（404）：`{"detail": "strategy_id=X 不存在"}`

##### 更新策略

```
PUT /strategies/{strategy_id}
Content-Type: application/json
```

**请求体**（所有字段可选，支持部分更新）：
```json
{
  "name": "更新后的名称",
  "rules": { "depth": 2 }
}
```

**响应**（200）：`StrategyOut`

##### 删除策略

```
DELETE /strategies/{strategy_id}
```

**响应**（200）：
```json
{"msg": "strategy deleted", "strategy_id": 1}
```

#### 3.1.3 爬虫控制接口

##### 启动爬虫

```
POST /crawl/start
Content-Type: application/json
```

**请求体**：
```json
{"strategy_id": 1}
```

**响应**（200）：
```json
{
  "msg": "crawl started",
  "task_id": 42,
  "strategy_id": 1
}
```

**错误**：
- 400：`{"detail": "已有爬虫任务在运行"}` 或 `{"detail": "该策略当前不是 enabled 状态"}`
- 404：`{"detail": "strategy_id=X 不存在"}`

##### 暂停爬虫

```
POST /crawl/pause
```

**响应**：`{"msg": "paused"}`

##### 恢复爬虫

```
POST /crawl/resume
```

**响应**：`{"msg": "resumed"}`

##### 停止爬虫

```
POST /crawl/stop
```

**响应**：`{"msg": "stop signal sent"}`

##### 查询爬虫状态

```
GET /crawl/status
```

**响应**：
```json
{
  "thread_alive": true,
  "paused": false,
  "stopped": false,
  "current_strategy_id": 1
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `thread_alive` | bool | 爬虫线程是否存活 |
| `paused` | bool | 是否处于暂停状态 |
| `stopped` | bool | 是否已发送停止信号 |
| `current_strategy_id` | int/null | 当前运行的策略 ID |

#### 3.1.4 任务查询接口

##### 获取任务列表

```
GET /tasks?strategy_id={strategy_id}
```

**查询参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `strategy_id` | int | 否 | 按策略筛选，不传则返回全部 |

**响应**：`TaskOut[]`

```json
[
  {
    "id": 42,
    "strategy_id": 16,
    "start_time": "2026-06-30T19:47:56",
    "Status": "completed",
    "end_time": "2026-06-30T19:48:01",
    "item_count": 5,
    "error_message": null
  }
]
```

##### 获取任务详情

```
GET /tasks/{task_id}
```

**响应**：`TaskOut`

### 3.2 数据代理接口（端口 8004）

#### 3.2.1 数据查询接口

##### 获取网页列表

```
GET /webpages
```

**响应**：
```json
[
  {
    "id": 69,
    "website_id": 9,
    "task_id": 44,
    "url": "https://k.sina.com.cn/article_xxx.html",
    "url_hash": "09a4fc9aef4a0b789d42697ebea7c561",
    "fetch_time": "2026-06-30T19:50:16",
    "http_status": 200,
    "process_status": "parsed",
    "page_type": "mixed",
    "error_message": null
  }
]
```

##### 获取内容列表

```
GET /contents
```

**响应**（含多表 JOIN 关联字段）：
```json
[
  {
    "id": 1,
    "webpage_id": 69,
    "datasource_id": 5,
    "Title": "文章标题",
    "text_body": "正文内容...",
    "publish_time": "2026-06-30T10:00:00",
    "keywords": "关键词1,关键词2",
    "crawl_time": "2026-06-30T19:50:16",
    "task_id": 44,
    "strategy_id": 16,
    "Publisher": "新浪新闻",
    "datasource_url": "https://news.sina.com.cn"
  }
]
```

##### 获取图片列表

```
GET /images
```

**响应**：
```json
[
  {
    "id": 1,
    "webpage_id": 69,
    "image_url": "https://xxx.com/image.jpg",
    "local_path": "./images/xxx.jpg",
    "description": "图片描述"
  }
]
```

##### 获取网站列表

```
GET /websites
```

**响应**：
```json
[
  {
    "id": 1,
    "domain": "news.sina.com.cn",
    "name": "news.sina.com.cn",
    "company_info": "新浪公司",
    "contact_info": "contact@sina.com"
  }
]
```

#### 3.2.2 数据删除接口

##### 删除单条内容

```
DELETE /contents/{content_id}
```

**响应**（200）：
```json
{"msg": "content deleted", "content_id": 1}
```

**错误**（404）：`{"detail": "内容不存在: 1"}`

##### 批量删除内容

```
POST /contents/batch-delete
Content-Type: application/json
```

**请求体**：
```json
{"ids": [1, 2, 3]}
```

**响应**（200）：
```json
{"msg": "batch delete done", "deleted_count": 3}
```

##### 删除单条图片

```
DELETE /images/{image_id}
```

**响应**（200）：
```json
{"msg": "image deleted", "image_id": 1}
```

##### 批量删除图片

```
POST /images/batch-delete
Content-Type: application/json
```

**请求体**：
```json
{"ids": [1, 2, 3]}
```

**响应**（200）：
```json
{"msg": "batch delete done", "deleted_count": 3}
```

#### 3.2.3 级联删除接口

##### 级联删除任务

```
DELETE /tasks/{task_id}
```

删除任务记录及其关联的所有网页、内容和图片。

**响应**（200）：
```json
{
  "msg": "task and related data deleted",
  "task_id": 42,
  "deleted_webpages": 5
}
```

##### 级联删除策略

```
DELETE /strategy/{strategy_id}/cascade
```

删除策略及其所有关联的任务、网页、内容和图片。

**响应**（200）：
```json
{
  "msg": "strategy and all related data deleted",
  "strategy_id": 16,
  "deleted_tasks": 3
}
```

#### 3.2.4 策略更新接口

```
PUT /strategies/{strategy_id}
Content-Type: application/json
```

**请求体**（所有字段可选）：
```json
{
  "name": "更新名称",
  "target_url": "https://new-url.com",
  "rules": {"depth": 2},
  "Status": "enabled",
  "Frequency": "manual",
  "creator_id": 1
}
```

**响应**（200）：更新后的策略完整对象（含解析后的 `rules` 字段）。

### 3.3 接口状态码说明

| 状态码 | 含义 | 场景 |
|--------|------|------|
| 200 | 成功 | 所有正常请求 |
| 400 | 请求错误 | 参数缺失/无效、爬虫已在运行 |
| 404 | 资源不存在 | 策略/任务/内容/图片 ID 不存在 |
| 422 | 校验错误 | 请求体不符合 Pydantic 模型 |
| 500 | 服务器错误 | 数据库异常、内部错误 |

---

## 四、数据模型说明

### 4.1 数据库表结构

| 表名 | 中文名 | 核心字段 | 关联关系 |
|------|--------|---------|---------|
| `admin` | 管理员 | id, Username, password_hash | → crawler_strategy |
| `crawler_strategy` | 爬虫策略 | id, name, target_url, rules_json, Status | → task_record |
| `task_record` | 任务记录 | id, strategy_id, Status, item_count | → webpage |
| `website` | 网站 | id, domain, name, company_info | → webpage |
| `webpage` | 网页（核心枢纽） | id, website_id, task_id, url, url_hash, fetch_time | ↔ website, task_record → content, image |
| `datasource` | 数据来源 | id, publisher_name, origin_url | → content |
| `content` | 文本内容 | id, webpage_id, Title, text_body, publish_time | ↔ webpage |
| `image` | 图片 | id, webpage_id, image_url, local_path | ↔ webpage |
| `system_setting` | 系统设置 | id, setting_key, setting_value | — |

### 4.2 核心关系链

```
admin (1) ──→ (N) crawler_strategy (1) ──→ (N) task_record (1) ──→ (N) webpage (1) ──→ (N) content
                                                                    ↓
website (1) ──→ (N) webpage (1) ──→ (N) image
                                    ↓
datasource (1) ──→ (N) content
```

`webpage` 表是核心枢纽：
- 向上关联 `website`（所属网站）和 `task_record`（所属任务）
- 向下关联 `content`（页面文本内容）和 `image`（页面图片）
- 唯一约束：`(website_id, url_hash)` 防止同一网站同一 URL 重复记录

---

## 五、使用示例

> 使用示例部分已由用户编写完成，此处不再重复。请参考用户操作手册中的"爬虫工具使用说明"章节获取完整的操作流程示例。

---

## 六、环境变量配置

系统支持通过环境变量覆盖默认配置：

| 环境变量 | 默认值 | 用途 | 配置位置 |
|----------|--------|------|---------|
| `DATABASE_URL` | `mysql+pymysql://root:...@172.31.224.1:3306/crawler_db` | 后端数据库连接 | CRAWLER_NEW.py |
| `DB_HOST` | `172.31.224.1` | 数据代理数据库主机 | data_proxy.py |
| `DB_PORT` | `3306` | 数据代理数据库端口 | data_proxy.py |
| `DB_USER` | `root` | 数据代理数据库用户 | data_proxy.py |
| `DB_PASSWORD` | `zhouwanyi1222` | 数据代理数据库密码 | data_proxy.py |
| `DB_NAME` | `crawler_db` | 数据代理数据库名 | data_proxy.py |
| `DATA_PROXY_PORT` | `8004` | 数据代理服务端口 | data_proxy.py |

**使用示例**（Linux/Mac）：
```bash
export DB_HOST=127.0.0.1
export DB_PASSWORD=your_password
python CRAWLER_NEW.py
```

**使用示例**（Windows PowerShell）：
```powershell
$env:DB_HOST="127.0.0.1"
$env:DB_PASSWORD="your_password"
python CRAWLER_NEW.py
```

---

*本文档基于项目当前版本代码编写，涵盖模块描述、接口说明与环境配置。*
